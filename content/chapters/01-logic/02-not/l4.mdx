---
level: l4
title: Negation
readingTimeMin: 5
newTerms: ["negation"]
anchorExample: "x ≥ 1"
---

import TruthTable from '@/components/TruthTable.astro';
import Warning from '@/components/Warning.astro';

For any statement $p$, its **negation** — written $\neg p$ — is the statement that is true exactly when $p$ is false. Negation flips the truth value.

## Truth table

<TruthTable
  variables={["p"]}
  expressions={["¬p"]}
  rows={[
    { p: true,  "¬p": false },
    { p: false, "¬p": true }
  ]}
/>

Two immediate consequences:

- $p$ and $\neg p$ are never both true.
- $p$ and $\neg p$ are never both false.

So one of them is always true and the other always false — exactly one.

Two consequences follow, and both become proof tools later:

- A claim and its denial can **never both hold**. If an argument ever forces you into both at once, whatever you assumed cannot have been true — and that is a whole proof technique.
- One of the two **always** holds. That is what lets you split an argument into the case where $p$ is true and the case where it is false, with nothing left over.

Neither is a technicality; module 1.3 gives both a name once the symbols for *and* and *or* are available.

## Double negation

If negating flips the truth value, then negating twice puts it back. That is,
$$
\neg\neg p \text{ has the same truth value as } p.
$$

*Symbol by symbol: $\neg$ is "not". So the line reads: denying a statement twice gets you back to the statement you started with.*

This looks obvious but it is a real fact worth naming: *double negation is the identity*. It shows up whenever you find yourself in a contradiction proof (§2.5).

It is also the quiet last step of every such proof. You want to show $q$. You assume $\neg q$, reach a contradiction, and conclude that $\neg q$ is false — which is to say $\neg\neg q$. Then you write down $q$ and move on. The replacement is double negation, and most write-ups never mention it.

## Negating with variables

- Negation of $x \geq 1$ is $x < 1$. (Note the flip *and* the change from $\geq$ to $<$.)
- Negation of $x \neq 0$ is $x = 0$.
- Negation of "$n$ is even" is "$n$ is odd" — but only because every integer is one or the other. Without that background fact, the negation would just be "$n$ is not even."

That last one is worth pausing on. *Not even* and *odd* mean the same thing for integers and not for other kinds of numbers: $1/2$ is not even, and it is not odd either. Whenever you replace a negation with a positive-sounding word, you are using a fact about what the possibilities are, and that fact deserves to be stated.

<Warning title="Common mistake">
The negation of $x \geq 1$ is $x < 1$, *not* $x \leq 1$. The negation of $x > 1$ is $x \leq 1$, not $x < 1$. The boundary case flips with the inequality.
</Warning>

## Negating quantities

- Negation of *there are infinitely many primes* is *there are only finitely many primes*.
- Negation of *every number is even* is *at least one number is not even*.

Both illustrate that to negate a universal claim you produce one exception — this is the "one bad apple" that module 1.11 will make formal.

The reverse direction is less familiar and just as useful. To negate *there exists a thing with this property*, you must rule out every candidate — which is a universal claim, and so needs a genuine argument rather than a single example. Disproving an existence claim is the harder of the two jobs, and knowing which one you are facing tells you what kind of work to expect.

## Denying an inequality, carefully

Inequalities are where the boundary case does the most damage, so it is worth working through all four of them once.

| Statement | Its denial |
|---|---|
| $x > 5$ | $x \leq 5$ |
| $x \geq 5$ | $x < 5$ |
| $x < 5$ | $x \geq 5$ |
| $x \leq 5$ | $x > 5$ |

Read down the table and you can see the pattern: **the inequality reverses, and the boundary point changes sides.** The number $5$ itself belongs to exactly one of any statement-and-denial pair, which is the whole content of module 1.1's insistence that a statement is true or false and never both.

The mistake to avoid is denying $x > 5$ with $x < 5$. That leaves $5$ itself in neither one, which would mean the original statement is neither true nor false at $x=5$ — and it is plainly false there.

## Two denials that look alike and are not

> **"Not everyone in the class passed."**

This says at least one person failed. It does **not** say everyone failed. The first is a modest claim; the second is a strong one, and English lets you slide between them without noticing.

> **"Everyone in the class did not pass."**

This one says everybody failed, though a careless reader takes it for the first sentence. Mathematics cannot afford that ambiguity, which is why module 1.11 fixes the two shapes down with symbols instead of word order.

Getting these two apart is worth the effort now, because almost every proof by contradiction (§2.5: assume the opposite and derive nonsense) starts by denying something, and a proof that denies the wrong statement proves nothing at all.
