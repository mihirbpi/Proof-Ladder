---
level: l7
title: Bijections and size
readingTimeMin: 10
newTerms: ["bijection", "identity function", "size", "listable"]
anchorExample: "a rule with a declared domain and codomain"
---

import Discussion from '@/components/Discussion.astro';
import Proof from '@/components/Proof.astro';
import Aside from '@/components/Aside.astro';
import Warning from '@/components/Warning.astro';
import WhereThisGoes from '@/components/WhereThisGoes.astro';

$f : S \to T$ is a **bijection** when it is injective and surjective, equivalently when every $f^{-1}(\{t\})$ is a singleton.

| | $\lvert f^{-1}(\{t\})\rvert$ |
|---|---|
| injective | $\leq 1$ |
| surjective | $\geq 1$ |
| bijective | $=1$ |

## Bijections as a renaming that preserves all the structures

> **Proposition.** $f : S \to T$ is a bijection iff there exists $g : T \to S$ with $g \circ f = \mathrm{id}_S$ and $f \circ g = \mathrm{id}_T$.

This is §3.9's theorem, but it is worth stating here because it is the *right* definition: a bijection is a function with a two-sided inverse, i.e. an **a renaming that preserves all the structure** in the category of sets. Under this reading the facts below are automatic — identities are a renaming that preserves all the structures, composites of a renaming that preserves all the structures are a renaming that preserves all the structures, and a renaming that preserves all the structure is an equivalence relation.

$\mathrm{id}_S$ is a bijection, and by §3.7 a composite of bijections is a bijection.

> **Proposition.** $\exp : \mathbb{R}\to\mathbb{R}_{>0}$ is a bijection.

<Proof>
*Injective.* $\exp$ is strictly increasing (as $\exp' = \exp > 0$), and strictly monotone — always heading the same way, never turning back — functions are injective (§3.5).

*Surjective.* Given $t > 0$, $x = \ln t \in \mathbb{R}$ satisfies $e^x = t$. Alternatively, $\exp$ is continuous — unbroken, drawable without lifting the pen — with $\lim_{x\to-\infty}e^x = 0$, $\lim_{x\to\infty}e^x=\infty$, so the intermediate value theorem gives every positive value.
</Proof>

Changing the codomain to $\mathbb{R}$ destroys surjectivity and so bijectivity — the same rule, a different function (§3.1).

## Comparing sizes

Define $S \sim T$ ("the same size") when a bijection $S \to T$ exists. This is an equivalence relation (§7.2): reflexive by $\mathrm{id}$, symmetric by inverses, transitive by composition — the three facts above, restated.

**Cardinality** $|S|$ is the equivalence class (§7.2: the family of things counted as the same), and $|S| = |T|$ means $S \sim T$. For finite sets this agrees with counting; the content is that it continues to make sense when counting does not.

Order is defined by $|S| \leq |T|$ iff an injection $S \to T$ exists. That this is such that mutual containment forces equality — $|S|\leq|T|$ and $|T|\leq|S|$ imply $|S|=|T|$ — is a named theorem in its own right, and it is genuinely not obvious: from injections both ways one must *construct* a bijection, and the construction (partitioning by orbits of the two maps) is the standard first surprise of the subject.

## An infinite set can match up with part of itself

> **Proposition.** $|\mathbb{N}| = |2\mathbb{N}|$.

<Proof>
$f : \mathbb{N}\to 2\mathbb{N}$, $f(n)=2n$. Injective: $2a=2b \Rightarrow a=b$. Surjective: any even natural is $2k$, and $f(k)=2k$.
</Proof>

$2\mathbb{N} \subsetneq \mathbb{N}$ yet the two have the same size. That is not a paradox, it is the definition of infinite doing its job: a set is infinite exactly when it can be matched one-for-one with a part of itself that leaves something out. No finite set can do that, and every infinite one can. It was proposed as *the* definition of infinity in 1888, and it is the places set theory's subtleties surface early.)

<Warning title="Inclusion is not size">
For infinite sets, $A \subsetneq B$ does not imply $|A| < |B|$. Comparisons must be made by exhibiting functions, never just by looking or by inclusion. Nearly every incorrect claim students make about infinite cardinalities (sizes, §3.8) comes from importing finite intuition here.
</Warning>

## Listable and not

$S$ is **listable** when its members can be written out in a list, one after another. Listable sets include $\mathbb{Z}$ (interleave positives and negatives), $\mathbb{N}\times\mathbb{N}$ (diagonal enumeration), and so $\mathbb{Q}$ — despite $\mathbb{Q}$ being dense in $\mathbb{R}$, which is a good demonstration that counting size and being densely packed measure different things.

> **Theorem (Cantor).** $\mathbb{R}$ is not listable.

<Discussion>
  <div slot="know">Every real in $(0,1)$ has a decimal expansion, unique if we forbid trailing nines.</div>
  <div slot="want">No surjection $\mathbb{N}\to(0,1)$ exists.</div>
  <div slot="how">Suppose not (§2.5). Given a claimed enumeration, construct a real differing from the $n$-th listed number in its $n$-th digit. It cannot be on the list, contradicting surjectivity.</div>
</Discussion>

<Proof>
Suppose, for contradiction, that $f : \mathbb{N}\to(0,1)$ is surjective. Write the decimal expansion of $f(n)$ as $0.a_{n1}a_{n2}a_{n3}\ldots$, choosing the expansion without trailing nines.

Define $b_n = 5$ if $a_{nn} \neq 5$, and $b_n = 4$ if $a_{nn} = 5$, and set $x = 0.b_1b_2b_3\ldots$. Then $x \in (0,1)$, and its expansion contains no nines and is not eventually zero, so it is the unique expansion of $x$.

For each $n$, $x$ differs from $f(n)$ in the $n$-th decimal place, so $x \neq f(n)$. So $x \notin \operatorname{Im}(f)$, contradicting surjectivity.

Therefore no list can contain them all, so the reals are not listable.
</Proof>

The care with trailing nines is not fussiness: $0.4999\ldots = 0.5$, and without excluding one of the two expansions the constructed $x$ might coincide with a listed number.

The same move proves more. For any set $S$ at all, $|S| < |\mathcal{P}(S)|$: given a proposed matching $f$, collect $D = \{s : s \notin f(s)\}$ and check that nothing is sent to it (§2.3, and §6.7 does it in full). So there is no largest size of infinity — they go up forever.

<Aside>
What is worth carrying out of this module is not the theorem but the move: **when someone hands you a list and claims it is complete, build the thing that disagrees with entry $n$ in position $n$.** That one trick is why $\mathbb{R}$ cannot be listed, why no set has as many members as it has subsets (§6.7), and why §2.1 could show there is no set of everything. Three different results, one idea, and the idea is the part that transfers.
</Aside>

<WhereThisGoes level="l7">
**The dimension theorem is a bijection theorem.** Two finite-dimensional spaces are the same up to renaming if and only if they have the same dimension, which is this module's *bijection means same size* with structure added.

**In calculus, bijectivity is what makes inverse functions exist.** $\ln : (0,\infty)\to\mathbb{R}$ and $e^x : \mathbb{R}\to(0,\infty)$ are mutually inverse bijections, and the domains are exactly what makes them so. So are $\tan$ on $(-\pi/2,\pi/2)$ and $\tan^{-1}$ on $\mathbb{R}$.

Each of those restrictions was chosen to manufacture a bijection out of a function that was not one, which is the standard repair and the reason inverse trigonometric functions come with ranges you have to memorize.

</WhereThisGoes>
