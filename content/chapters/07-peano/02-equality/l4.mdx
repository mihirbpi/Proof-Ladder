---
level: l4
title: The rules for equality
readingTimeMin: 6
newTerms: ["reflexive", "symmetric", "transitive", "equivalence relation"]
anchorExample: "successor function"
---

import Proof from '@/components/Proof.astro';

## Axioms 1–4

For all $x, y, z$:

$$
\textbf{A1 (reflexivity)}\quad x = x
$$

*In words: everything equals itself; if one thing equals a second, the second equals the first; and equalities chain.*
$$
\textbf{A2 (symmetry)}\quad x = y \Rightarrow y = x
$$

*Symbol by symbol: $\Rightarrow$ is "forces". So the line reads: if $x$ equals $y$, then $y$ equals $x$.*

$$
\textbf{A3 (transitivity)}\quad (x=y \wedge y=z) \Rightarrow x = z
$$

*Symbol by symbol: $\Rightarrow$ is "forces", $\wedge$ is "and". So the line reads: if $x$ equals $y$ and $y$ equals $z$, then $x$ equals $z$.*

$$
\textbf{A4 (closure)}\quad (x \in \mathbb{N} \wedge x = y) \Rightarrow y \in \mathbb{N}
$$

*Symbol by symbol: $\mathbb{N}$ is the counting numbers, $\in$ is "is in", $\Rightarrow$ is "forces", $\wedge$ is "and". So the line reads: if $x$ is a counting number and $x$ equals $y$, then $y$ is a counting number too.*

A4 is the *substitution* property in miniature: equals may be substituted for equals in any statement, here specifically in membership claims. Without it, "equal" objects could differ in which sets they belong to, and every proof that rewrites one expression as an equal one would be unjustified.

## Equivalence relations

A relation $\sim$ on a set $S$ satisfying A1–A3 is an **equivalence relation**. Recall the move module 2.9 made, since it is easy to skim past: a relation on $S$ is not treated as a description but as the **collection of ordered pairs it holds for**. So $a \sim b$ is shorthand for *the pair $(a,b)$ is in that collection*. The reason for defining it that way is that two descriptions holding for exactly the same pairs can never be told apart by any proof — so the pairs are the only part that matters.

### Which relations qualify

| Relation | Refl. | Symm. | Trans. | Equivalence? |
|---|---|---|---|---|
| $=$ on any set | ✓ | ✓ | ✓ | yes |
| same remainder on division by $n$ | ✓ | ✓ | ✓ | yes |
| same cardinality (module 3.8) | ✓ | ✓ | ✓ | yes |
| $\leq$ on $\mathbb{R}$ | ✓ | ✗ | ✓ | no |
| $<$ on $\mathbb{R}$ | ✗ | ✗ | ✓ | no |
| $\subseteq$ (module 2.3) | ✓ | ✗ | ✓ | no |
| "is a friend of" | ✓? | ✓ | ✗ | no |
| $\perp$ (perpendicular) | ✗ | ✓ | ✗ | no |

### Relations that fail

Note that $\leq$ and $\subseteq$ satisfy two of the three and are **orderings** instead — reflexive, transitive, and *such that mutual containment forces equality*. Swapping symmetry for the rule that mutual containment forces equality gives an entirely different kind of structure, which is why the three properties must be checked individually.

## Same remainder

For $n \geq 1$, say that $a$ and $b$ **leave the same remainder on division by $n$** when $n \mid (a-b)$ (module 4.2). Later courses give this a symbol of its own and a name — *congruence* — but the notation is theirs, and the idea is the one you already have from long division.

<Proof>

*The symbols in this proof: $\in$ is "is in", $\mathbb{Z}$ is the integers, the bar $\mid$ is "divides". In words, what follows shows: having the same remainder passes all three tests, so it behaves like equality.*

**Reflexive.** $n \mid (a - a) = 0$ for every $n$, so $a$ leaves the same remainder as itself.

**Symmetric.** If $n \mid (a-b)$, say $a - b = nk$, then $b - a = n(-k)$ with $-k \in \mathbb{Z}$, so $n \mid (b-a)$.

**Transitive.** If $a - b = nj$ and $b - c = nk$, then
$$a - c = (a-b)+(b-c) = n(j+k),$$
and $j+k \in \mathbb{Z}$ by closure. So $n \mid (a-c)$.
</Proof>

Each step is the divisibility template of module 4.3.

## Where else the three properties show up

The point of naming the three properties is that they turn up constantly, and spotting them saves work.

| Relation | Reflexive? | Symmetric? | Transitive? |
|---|---|---|---|
| *is the same age as* | ✓ | ✓ | ✓ |
| *leaves the same remainder on division by $3$* | ✓ | ✓ | ✓ |
| *is the same height as* | ✓ | ✓ | ✓ |
| *is taller than* | ✗ | ✗ | ✓ |
| *is a sibling of* | ✗ | ✓ | ✗ |

The first three behave like equality and can be used like it. The last two cannot, and the table shows exactly which property fails in each case.

*Is a sibling of* is the one worth doing carefully. It feels like a "same as" relation — same parents, after all — but you are not your own sibling, so it fails reflexivity. Test each property separately, every time, rather than deciding by how the relation sounds.

## What "behaves like equality" buys you

When a relation passes all three tests, you can reason with it the way you reason with $=$: swap one side for the other, chain statements together, and run the argument in either direction.

That is the whole reason for the checking. Take *leaves the same remainder on division by $3$*. Because it passes the three tests, you may argue that $17$ and $2$ leave the same remainder, and $2$ and $11$ leave the same remainder, so $17$ and $11$ do — without dividing anything. Transitivity is what licenses that step, and if the relation had failed it the step would be worthless.

Module 4.4 uses this constantly. Splitting a proof into the cases *remainder $0$*, *remainder $1$*, *remainder $2$* is only legitimate because every integer leaves exactly one of those remainders, and that fact is these three properties doing their work.
