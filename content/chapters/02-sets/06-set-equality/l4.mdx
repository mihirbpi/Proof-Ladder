---
level: l4
title: Set equality by double inclusion
readingTimeMin: 8
newTerms: ["double inclusion"]
anchorExample: "roots of sin x and cos x"
---

import Discussion from '@/components/Discussion.astro';
import Proof from '@/components/Proof.astro';
import Aside from '@/components/Aside.astro';
import Warning from '@/components/Warning.astro';

## The definition

$$
S = T \quad:\Leftrightarrow\quad S \subseteq T \;\wedge\; T \subseteq S.
$$

*Symbol by symbol: $\subseteq$ is "sits inside", $\Leftrightarrow$ is "exactly when", $\wedge$ is "and". So the line reads: **$S$ and $T$ are the same collection exactly when each sits inside the other.*

Set equality is **double inclusion**, and the named move is **check both boxes**. Every set-equality proof in mathematics has two halves, and they are usually different in difficulty and technique.

The definition is not an arbitrary convention. A set is fixed by its members and nothing else (module 2.1): $S = T$ iff $\forall x\,(x \in S \Leftrightarrow x \in T)$. An if-and-only-if statement is two implications (module 1.9), and each implication is a subset claim. So the two inclusions were already inside the definition of equality.

## Template

> **($\subseteq$)** Let $x \in S$ be arbitrary. … Hence $x \in T$.
> **($\supseteq$)** Let $x \in T$ be arbitrary. … Hence $x \in S$.
> Therefore $S = T$. $\square$

Label the halves. Unlabeled proofs of equalities routinely contain the same direction twice.

## Disjunction forces cases

Since $x \in S \cup T$ unfolds to $x \in S \vee x \in T$, chasing an element out of a union hands you an *or* — and an *or* in the hypothesis must be handled by **proof by cases** (module 1.12). You do not get to choose whichever half is convenient.

This single observation predicts the structure of most proofs in the rest of the chapter.

## A worked proposition

Let
$$
S = \{x \in \mathbb{R} \mid x - 2 = 0\}, \qquad
T = \{x \in \mathbb{R} \mid x + 3 = 0\}, \qquad
R = \{x \in \mathbb{R} \mid (x-2)(x+3) = 0\}.
$$

*Symbol by symbol: $\mathbb{R}$ is the real numbers, $\in$ is "is in", the bar $\mid$ is "such that".*

> **Proposition.** $S \cup T = R$.

<Discussion>
  <div slot="know">$x \in S \cup T$ means $x - 2 = 0$ or $x + 3 = 0$. $x \in R$ means the product $(x-2)(x+3)$ is zero. The relevant fact about $\mathbb{R}$: a product of two reals is zero if and only if at least one factor is zero.</div>
  <div slot="want">Both inclusions.</div>
  <div slot="how">($\subseteq$) The hypothesis is a disjunction, so split into cases; in each, one factor is zero and the product is too. ($\supseteq$) The hypothesis is a product being zero, so the zero-product fact hands us a disjunction directly.</div>
</Discussion>

<Proof>
**($\subseteq$)** Let $x \in S \cup T$ be arbitrary. Then $x - 2 = 0$ or $x + 3 = 0$; we split into cases.

*Case 1: $x - 2 = 0$.* Then $(x-2)(x+3) = 0 \cdot (x+3) = 0$.

*Case 2: $x + 3 = 0$.* Then $(x-2)(x+3) = (x-2) \cdot 0 = 0$.

In both cases $(x-2)(x+3) = 0$, so $x \in R$. The cases are exhaustive.

**($\supseteq$)** Let $x \in R$ be arbitrary, so $(x-2)(x+3) = 0$. In $\mathbb{R}$ a product is zero only if a factor is: $x - 2 = 0$ or $x + 3 = 0$. Hence $x \in S$ or $x \in T$, i.e. $x \in S \cup T$.

Both inclusions hold, so $S \cup T = R$.
</Proof>

The two halves used different facts: the forward direction used only that $0 \cdot a = 0$; the backward direction used the substantive fact that a product of reals cannot be zero unless a factor is. That fact is not automatic. On a six-hour clock, where the hours run $0$ to $5$ and adding wraps around, $2 \times 3$ lands on $0$ with neither factor zero — and there the backward inclusion would be false. Noticing which structural fact each half consumes is how you learn where a theorem lives.

Note also that $S$ and $T$ are disjoint (module 2.4), so $R$ is partitioned by them — but disjointness played no role in the proof above. Do not import hypotheses you do not need.

## A second example: absorption

> **Proposition.** For all sets $S, T$: $S \cup (S \cap T) = S$.

<Proof>
**($\subseteq$)** Let $x \in S \cup (S \cap T)$. Then $x \in S$ or $x \in S \cap T$.

*Case 1: $x \in S$.* Done.

*Case 2: $x \in S \cap T$.* Then $x \in S$ and $x \in T$, so $x \in S$.

Either way $x \in S$.

**($\supseteq$)** Let $x \in S$. Then "$x \in S$ or $x \in S \cap T$" holds, since its left half does. So $x \in S \cup (S \cap T)$.
</Proof>

Observe the asymmetry that recurs throughout: proving membership in a *union* is easy (establish either half), while *using* membership in a union is expensive (case split). Intersection is the mirror image — using it is free, proving it costs two arguments.

<Warning title="Three failure modes">
**Proving one direction.** The commonest. Label your halves and count them.

**Reversible-step drift.** Writing a chain $x \in S \Leftrightarrow \cdots \Leftrightarrow x \in T$ is legitimate only if every step is genuinely if-and-only-if statement. It usually is in set theory, since the definitions are if-and-only-if statements — but you must check, and stating two explicit inclusions is safer.

**Skipping a case.** If the hypothesis is an *or*, both halves need treatment. A missing case is a hole, not a stylistic lapse.
</Warning>

<Aside>
Double inclusion also finishes something module 2.3 started. There, $\subseteq$ was shown to behave like $\leq$ does on numbers: every set contains itself, and containment passes along a chain. The one missing piece was that two sets containing each other must be equal — and that is this module's definition, read backwards.
</Aside>
